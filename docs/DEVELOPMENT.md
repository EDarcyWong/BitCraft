# 开发与验证

## 当前架构

| 模块 | 职责 |
| --- | --- |
| `src/core/circuit.ts` | 元件与导线数据模型、组合逻辑求值、连接合法性、导入电路校验 |
| `src/core/levels.ts` | 六个关卡、固定端口、元件范围、真值表判定与提示 |
| `src/core/storage.ts` | v1 作品文件与本地存档解析，数据规模与格式约束 |
| `src/composables/useWorkshop.ts` | 电路编辑、50 步撤销历史、关卡草稿、自动保存、作品库 |
| `src/components/CircuitCanvas.vue` | SVG 元件、导线、坐标变换、接线、拖动、缩放与键盘交互 |
| `src/App.vue` | 工坊、挑战手册、元件库、作品库、导入导出和通关反馈 |

引擎按依赖顺序求值，输入缺失时传播 `null`（未定义），相同输入得到相同结果。导线动画只表示信号状态，不参与逻辑计算。当前不模拟时钟、反馈、真实传播延迟或模拟电路。

SVG 适合当前 128 个元件的有限工作台，也让引脚具有可访问名称和键盘操作。求值代码没有导入 Vue 或 DOM，后续可独立扩展渲染器或模拟规则。

## 存档与错误处理

- 进度、沙盒草稿、每关草稿和作品库保存在 `bitcraft:studio:v1`。
- 编辑后延迟 350 毫秒保存；页面隐藏或退出时提交最后的状态。
- 作品文件标记为 `format: "bitcraft-circuit"`、`version: 1`，只包含名称和电路数据。
- 导入先校验所有元件和连线，成功后再替换工作台；不支持的版本、未知元件、重复编号、无效坐标、重复驱动和环路均被拒绝。
- 本地存档损坏时显示提示，暂停自动覆盖；主动保存作品时先保留原数据到恢复备份键。
- 存储空间不足或浏览器禁止存储时，界面显示保存失败，仍可继续搭建并导出 JSON 文件。
- 撤销历史属于本次编辑会话，不写入长期存档。切换关卡或重新打开作品会清空撤销历史。

## 自动检查

```bash
npm ci
npm test
npm run build
```

Vitest 目前覆盖 42 项测试：逻辑门全部输入、断线与未定义传播、扇出、乱序依赖、128 元件长链、非法接线、组合环路、完整六关、固定端口约束、禁止提前使用元件、导入导出和存档恢复。

生产构建先执行严格 TypeScript 检查。GitHub Actions 在 Linux 和 Windows 上使用 Node.js 22 执行依赖安装、测试及构建。

## 浏览器验收记录

2026-09-14，在本地桌面浏览器实际操作完成：

- 切换示例开关，确认输出灯和导线信号实时变化。
- 逐关搭建并完成全部六个关卡，包括用 NOT、AND、OR 组合实现 XOR，以及半加器的和与进位输出。
- 点击接线与拖拽接线；重复驱动被拒绝且原连接保留。
- 拖动元件、修改元件名称、删除元件并撤销，电路恢复后仍通过验证。
- 未接通的电路不能通关，失败时显示未定义结果与原因。
- 保存半加器作品并刷新，作品、布局、关卡进度均恢复，作品可在沙盒重新打开。
- 导入非法元件文件被拒绝，原电路不变；合法作品导入后立即正确求值。
- 720 像素高桌面窗口中，工作台底部工具保持可见，侧栏内容在内部滚动。
- 900 × 720 窄桌面窗口中可展开手册，页面无横向或纵向溢出。
- 导入 128 元件、127 连线的长链电路，全部元件正常呈现，末端输出与预期一致。

测试使用 `127.0.0.1:5177`；交付试玩入口使用 `localhost:5177`，两者的浏览器存档相互独立。

## 后续边界

模块封装、时钟、寄存器、总线、CPU、汇编编辑器、云端存档与完整触屏体验尚未实现。当前本地状态不适用于多人协作，也不在不同浏览器标签页之间实时同步。
